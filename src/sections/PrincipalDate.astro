<section
  class="mt-32 animate-fade-in animate-delay-700 mx-auto flex flex-col justify-center items-center text-center text-primary"
>
  <h2 class="uppercase text-5xl font-semibold">
    4 de marzo<span aria-hidden class="hidden md:inline mx-1">·</span><br
      aria-hidden
      class="block md:hidden"
    />
    <span>
      <script is:inline>
        const END_DATE = 1709575200000;
        const d = new Date(END_DATE);
        const date = d.toLocaleTimeString("es", {
          hour: "2-digit",
          minute: "2-digit",
        });

        // get currnt timezone
        const tzOffset = d.getTimezoneOffset() / 60;
        const diff = tzOffset * -1;
        const gmt = diff > 0 ? `GMT+${diff}` : `GMT-${diff}`;

        const TZ_DICTIONARY = {
          "GMT+1": "CET",
        };
        const tz = TZ_DICTIONARY[gmt] ?? gmt;
        const selfScript = document.currentScript;
        selfScript.parentNode.innerHTML = `${date}H ${tz}`;
      </script>
    </span>
  </h2>
  <h3 class="uppercase text-2xl font-medium flex flex-col max-w-sm mt-6">
    <span>Evento de Presentación</span>
    <span></span>
    <a class="hover:scale-110 transition inline-block flex flex-row" href="https://www.google.com/maps/dir//Av.+del+Paral%C2%B7lel,+67,+Sants-Montju%C3%AFc,+08004+Barcelona/@41.3747462,2.0863877,12z/data=!4m8!4m7!1m0!1m5!1m1!1s0x12a4a25c0c49f739:0x54e8eb6c24a94762!2m2!1d2.1687885!2d41.3747756?entry=ttu" target="_blank" rel="noopener noreferer">Teatro Victoria (Barcelona)</a>
  </h3>
  <a
    class={`
  skew-x-6 border-primary border-2 font-medium uppercase text-lg px-4 py-1 mt-8 hover:bg-primary hover:text-secondary hover:scale-125 transition inline-block
  `}
    href="https://www.teatrevictoria.com/es/cartelera/c/190_la-velada-del-ano-4.html"
    target="_black"
    rel="noopener noreferrer">Entradas agotadas</a
  >
</section>
